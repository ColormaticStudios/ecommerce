export function productRailKey(index: number): string {
	return `product_rail:${index}`;
}

export function categoryTilesKey(index: number): string {
	return `category_tiles:${index}`;
}

export function inventoryMessageKey(index: number): string {
	return `inventory_message:${index}`;
}
